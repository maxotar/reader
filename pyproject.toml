[project]
name = "reader-tools"
version = "0.1.0"
description = "EPUB preprocessing tools for the ESP32 reader project"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "lxml>=5.3.0",
]

[tool.uv]
package = false
